language: python

sudo: false

python:
  - 3.5
  - 3.6

install:
  - pip install tox-travis
  - pip install pytest-cov
  - pip install coveralls
  - pip install -e .

script: tox

after_success: coveralls
