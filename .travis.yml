language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
install:
  - "pip install -r requirements.txt"
  - "pip install pytest pytest-cov pytest-pep8 pytest-sugar codecov"
  - "pip install -e ."
script:
  - py.test --cov=./
after_success:
  - codecov
